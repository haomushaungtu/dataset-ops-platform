# Copy to a root- or dfmetadata-owned 0600 file outside the repository.
# Use an isolated PoC administrator only; never commit a real password.
OM_POC_EMAIL=replace-at-deploy-time
OM_POC_PASSWORD=replace-through-secure-runtime-file
